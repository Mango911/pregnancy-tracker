# API 后端地址
#
# 说明：
# - .env.development: 本地开发环境 (npm run dev / bun run dev)
# - .env.production:  生产构建环境 (npm run build / bun run build)
#
# 根据需要修改对应文件中的 VITE_API_URL

VITE_API_URL=http://localhost:8787
