{
  "name": "pregnancy-tracker-backend",
  "version": "1.0.0",
  "description": "Cloudflare Workers backend for pregnancy tracker",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute pregnancy-tracker-db --file=./schema.sql",
    "db:local": "wrangler d1 execute pregnancy-tracker-db --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "wrangler": "^3.22.0",
    "typescript": "^5.3.3"
  }
}
